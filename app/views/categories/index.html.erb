<h1> Categories General - index - </h1>
<% @categories.each do |category|  %>
  
  <h2><%= link_to category.name, category_path(category) %></h2>

  <% end  %>
<%= link_to "Add Category", new_category_path %>